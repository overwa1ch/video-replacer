# Local reference library

This directory is an ignored local asset library. Add reusable reference images here and record them in `catalog.json`; the files and live catalog must never be committed.

`catalog.example.json` defines the empty public schema used by `./install` to initialize `catalog.json`.
