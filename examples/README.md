# Examples

These files show the batch contract without including media. Copy real source videos and reference images only into an ignored runtime batch under `workspace/video-loop/inbox/`; do not add media to `examples/`.

For one source video, rename the copied file with a deterministic order prefix such as `001-source.mp4`, copy references into `replacements/`, then adapt the two schema-v3 example files.
